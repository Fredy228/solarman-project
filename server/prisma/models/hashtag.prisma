model Hashtag {
    id String @id @default(auto()) @map("_id") @db.ObjectId

    tag  String           @unique
    name LocalizedContent

    portfolioIds String[]    @db.ObjectId
    portfolio    Portfolio[] @relation(fields: [portfolioIds], references: [id])

    @@map("hashtag")
}
