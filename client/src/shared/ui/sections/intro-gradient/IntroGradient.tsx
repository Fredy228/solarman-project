import { Box, Container } from "@mui/material";

type Props = {};

export default function IntroGradient(props: Props) {
  return (
    <Box component={"section"} className="w-full overflow-hidden">
      <Container maxWidth="xl">
        <Box></Box>
      </Container>
    </Box>
  );
}
